{
  "name": "SIM",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "nodemon --exec pnpm serve:electron",
    "start": "pnpm nightly:rename && concurrently -k \"pnpm serve:front\" \"pnpm watch\"",
    "front:setup": "cd frontend && pnpm",
    "electron:setup": "pnpm",
    "setup": "pnpm front:setup && pnpm electron:setup",
    "serve:front": "cd frontend && pnpm dev",
    "serve:electron": "set APP_IS_NIGHTLY=yes && set APP_IS_DEV=yes && pnpm build && wait-on tcp:3000 && electron .",
    "build:front": "cd frontend && pnpm build",
    "electron:build": "pnpm build",
    "electron:builder": "electron-builder",
    "app:build": "pnpm prod:rename && pnpm build:front && pnpm electron:build && pnpm electron:builder",
    "app:build:nightly": "set APP_IS_NIGHTLY=yes && set APP_IS_DEV=no && pnpm nightly:rename && pnpm build:front && pnpm electron:build && pnpm electron:builder",
    "nightly:rename": "json -I -f package.json -e \"this.name='SIM'\" && json -I -f package.json -e \"this.build.productName='SIM Nightly'\" && json -I -f package.json -e \"this.build.appId='com.sim.hack.app'\"",
    "prod:rename": "json -I -f package.json -e \"this.name='SIM'\" && json -I -f package.json -e \"this.build.productName='SIM'\" && json -I -f package.json -e \"this.build.appId='com.sim.hack.app'\"",
    "electron-rebuild": "./node_modules/.bin/electron-rebuild",
    "sqlite-rebuild": "cd node_modules/better-sqlite3 && npm run build-release",
    "postinstall": "electron-builder install-app-deps",
    "up:win": "up_using_window.cmd"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "build": {
    "appId": "com.sim.hack.app",
    "productName": "SIM",
    "copyright": "Copyright © 2022 ${author}",
    "publish": [
      {
        "provider": "github",
        "owner": "euberdeveloper"
      }
    ],
    "nsis": {
      "oneClick": true,
      "allowToChangeInstallationDirectory": false,
      "deleteAppDataOnUninstall": true
    },
    "files": [
      "dist/**/*"
    ],
    "extraResources": [
      "defaults/**/*"
    ],
    "directories": {
      "buildResources": "assets",
      "output": "dist_electron"
    }
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "electron-devtools-installer": "^3.2.0",
    "json": "^11.0.0",
    "nodemon": "^3.1.7",
    "typescript": "^5.6.3",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "electron-log": "^5.2.2",
    "electron-store": "^10.0.0",
    "electron-updater": "^6.3.9"
  }
}
